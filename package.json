{
  "name": "formlab-proxy",
  "version": "1.0.0",
  "description": "CORS proxy for Form Lab dashboard",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
