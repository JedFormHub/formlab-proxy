{
  "name": "formlab-proxy",
  "version": "2.0.0",
  "description": "Form Lab — CORS proxy + race scanner + results settler",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
